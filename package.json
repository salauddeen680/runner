{
  "name": "runner-game",
  "version": "1.0.0",
  "private": true,
  "description": "Runner - Run. Dodge. Survive.",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
